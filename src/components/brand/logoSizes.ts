/** Sidebar lockup — partner logo box; width scales from aspect ratio. */
export const SIDEBAR_LOGO_CLASS = 'h-10 max-w-[120px] shrink-0';

/** Sidebar app logo — slightly larger than partner for readability. */
export const SIDEBAR_APP_LOGO_CLASS = 'h-12 max-w-[140px] shrink-0';

/** Login lockup — larger display size, still proportional via object-contain. */
export const LOGIN_LOGO_CLASS = 'h-20 shrink-0';

export const LOGIN_PARTNER_LOGO_CLASS = 'max-w-[175px]';
export const LOGIN_APP_LOGO_CLASS = 'max-w-[230px]';
