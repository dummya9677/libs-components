/** Sidebar lockup — fits within 260px sidebar with partner + divider + app logo. */
export const SIDEBAR_LOGO_CLASS = 'h-12 min-w-0 max-w-full';

/** Sidebar app logo — same height as partner for a balanced lockup. */
export const SIDEBAR_APP_LOGO_CLASS = 'h-16 min-w-0 max-w-full';

/** Login lockup — larger display size, still proportional via object-contain. */
export const LOGIN_LOGO_CLASS = 'h-20 shrink-0';

export const LOGIN_PARTNER_LOGO_CLASS = 'max-w-[175px]';
export const LOGIN_APP_LOGO_CLASS = 'max-w-[230px]';
